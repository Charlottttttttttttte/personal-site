# 个人网站 · 视觉/交互设计规范（K3 交付）

> 状态：**K3 设计细化 v1.0**（2026-08-04）· 对应 DESIGN.md §1 任务范围，供 DeepSeek 直接实现
> 本文档覆盖 DESIGN.md §8 的全部 6 项决策。每项标注「推荐」；文末「待拍板」小节集中列出需 Charlotte 确认的点。

---

## 1. 视觉方向

### 1.1 风格基调（推荐：暗色科技感 × 杂志排版纪律 ——「墨·朱」主题）

在 DESIGN.md 给出的两个方向之间，选**暗色科技感**，但叠加杂志风的排版纪律（克制配色、细线分隔、无阴影渐变）。理由：

- 第一屏是 Three.js 3D 场景，深色底天然承载发光/线框元素，浅色底做 3D 氛围容易显脏
- 「墨色 + 朱砂」呼应人物背景中的书法项目，比通用科技蓝/紫更有个人辨识度
- 杂志纪律（≤3 色、单强调色、细线、无阴影）与 Charlotte 已有的知识卡片设计系统一致，站点与产出物料视觉统一

### 1.2 色板（CSS 变量，直接可用）

```css
:root {
  --ink:        #0E0F13;  /* 页面底色：深墨 */
  --ink-raise:  #16181F;  /* 卡片/浮层底：比页面浅一档 */
  --paper:      #E8E6E1;  /* 正文：暖灰白 */
  --paper-dim:  #9B988F;  /* 次级文字/注释 */
  --cinnabar:   #C73E2C;  /* 唯一强调色：朱砂（hover、进度、关键词） */
  --line:       rgba(232, 230, 225, 0.08); /* 分隔细线 */
}
```

纪律：

- 全站只有 `--cinnabar` 一个强调色，**每屏最多出现 1–2 处**
- 分隔一律用 1px `--line` 细线，禁用阴影、渐变、圆角卡片堆叠
- 圆角统一 `2px`（近乎直角，保持纸面感），按钮可用 `999px` 胶囊做对比

### 1.3 字体

| 用途 | 字体 | 说明 |
|:---|:---|:---|
| 标题 / 大数字 | Noto Serif SC（思源宋体）| 呼应书法气质的宋体笔画，Google Fonts CDN |
| 正文 / UI | Noto Sans SC（思源黑体）| 高可读性 |
| 数字 / 代码 / 标签 | JetBrains Mono | 天数计数、序号、标签一律等宽，强化"手账/仪表盘"感 |

字号阶梯（桌面）：Hero 名 64px / 区块标题 32px / 卡片标题 20px / 正文 16px / 注释 13px。移动端见 §5。

---

## 2. 3D 互动第一屏方案（推荐：「知识星群」）

### 2.1 场景内容

- **中心体**：一个线框二十面体（`IcosahedronGeometry`，EdgesGeometry 描边），线色 `--cinnabar`，透明度 0.9，缓慢自转（约 20s/圈）
- **环绕粒子**：60–80 个小发光点（`PointsMaterial`，`--paper` 色，大小 1.5–2.5 随机），分布在半径 2–4 的球壳上，轻微呼吸浮动（sin 位移 ±0.05）
- **5 个概念词条**：DESIGN.md §7 的 5 个概念（vibe coding / 3D 互动网页 / HTML 动画 / 响应式布局 / Agent 平台），用 `CSS2DRenderer` 或 sprite 文字挂在轨道上，等宽字体 13px，`--paper-dim` 色
- 背景：纯 `--ink`，不加星空贴图（保持克制）

### 2.2 交互规则

| 交互 | 行为 |
|:---|:---|
| 拖拽（鼠标/触摸） | 旋转整组（中心体+粒子+词条），带阻尼惯性（`dampingFactor 0.06`） |
| hover 词条 | 词条转 `--cinnabar` 并放大 1.15×，对应粒子簇亮度提升 |
| 点击词条 | 平滑滚动到对应区块（如"3D 互动网页"→ 数字花园概念笔记） |
| 无操作 3s | 恢复自动缓慢旋转 |
| 滚轮 | 不在 3D 区内做缩放，滚轮留给页面滚动（避免移动端滚动劫持） |

### 2.3 降级策略（移动端必须不塌）

- `<640px`：粒子数减半（30–40），词条减为竖排 3 个，自动旋转为主、触摸拖拽保留
- `prefers-reduced-motion` 或 WebGL 不可用：整屏降级为静态 SVG（线框二十面体 + 词条）+ CSS 淡入
- 首屏文案（名字 + 定位语）用 HTML 覆盖层实现，**不依赖 3D 加载**，3D 失败文案也完整可读

### 2.4 备选（若 Charlotte 想更极简）

仅中心线框多面体 + 粒子，去掉词条；交互只保留拖拽旋转 + hover 中心体变色。实现量减半。

---

## 3. GSAP 动效清单

统一约定：缓动默认 `power3.out`；区块入场时长 0.6–1.0s；stagger 0.08–0.12s；ScrollTrigger 默认 `start: "top 80%"`，`once: true`（入场动画不反复触发）。所有动画尊重 `prefers-reduced-motion`（命中时直接 `gsap.set()` 终态）。

| # | 区块 | 效果 | 触发 | 时长 / 缓动 |
|:--|:---|:---|:---|:---|
| 1 | 首屏 Hero 名字 + 定位语 | 逐字符上移浮现（`y: 24 → 0`, `opacity`） | 页面加载后（等字体 ready） | 0.8s / `power3.out`，stagger 0.04s |
| 2 | 首屏滚动引导箭头 | 循环轻 bounce（`y: 0 → 8 → 0`） | 加载后循环 | 1.6s / `sine.inOut`，repeat -1 |
| 3 | 「正在学」进度条 | 条宽 `0 → 目标%` 生长 + 天数 count-up（D11/28 数字滚动） | ScrollTrigger 进入视口 | 条 1.0s / `power3.out`；count 1.2s 同步 |
| 4 | 作品集卡片 | 整组 stagger fade-up（`y: 32 → 0`, `opacity`） | ScrollTrigger | 0.8s / `power3.out`，stagger 0.1s |
| 5 | 数字花园条目 | 逐条 fade-in + 左侧 1px 线从 0 生长到全高 | ScrollTrigger | 0.6s / `power2.out`，stagger 0.08s |
| 6 | 关于页段落 | 按段落行揭示（`clipPath` 或 `y+opacity`） | ScrollTrigger | 0.7s / `power2.out`，stagger 0.15s |
| 7 | Header | 滚动 >80px 时收缩：高度 72→56px，加 1px 底线 | ScrollTrigger `toggleClass` | 0.3s / `power1.out` |
| 8 | 区块标题序号（01/02/03…） | 等宽数字从透明滑入，先于标题 0.1s | ScrollTrigger | 0.5s / `power3.out` |

不做：视差多层、pin 固定滚动、水平滚动——三周工期 + 移动端稳定优先。

---

## 4. 组件样式规范

### 4.1 作品集卡片

```css
.work-card {
  background: var(--ink-raise);
  border: 1px solid var(--line);
  border-radius: 2px;
  padding: 24px;
  transition: border-color .25s ease, transform .25s ease;
}
.work-card:hover {
  border-color: var(--cinnabar);
  transform: translateY(-4px);   /* 无阴影，位移+描边色表达浮起 */
}
```

- 结构：左上**等宽序号**（`01.` `--paper-dim` 13px）→ 标题（宋体 20px）→ 一句话描述（`--paper-dim` 14px）→ 底部标签行（等宽 12px，细线分隔）+ 右下 `→` 箭头
- hover 时箭头 `--paper-dim → --cinnabar` 并右移 4px（CSS transition，不走 GSAP）
- 封面图（如有）：16:10，置于卡片顶部，与卡片同宽，无圆角，底下 1px `--line`
- 网格：`repeat(3, 1fr)`，gap 24px（桌面）

### 4.2 数字花园条目

- 列表式而非卡片：每条 = 1px 顶线 + 左侧概念名（宋体 18px）+ 右侧元信息（等宽 12px `--paper-dim`：类型/日期）
- hover：概念名左移 8px 并转 `--cinnabar`，行底出现 1px `--line` 延长线
- wiki 链接用 `↗` 后缀标识外部跳转

### 4.3 「正在学」状态条

- 两条并列（移动端上下堆叠），每条结构：
  - 标签行：`正在学 · 财经`（等宽 12px `--paper-dim`）+ 右侧 `D11/28`（等宽 12px，`--cinnabar`）
  - 进度轨：高 2px，底色 `--line`；进度填充 `--cinnabar`，宽度 = 11/28 ≈ 39%
  - 主题行：当前在学的一句话（正文 15px）
- Agent 平台一条无固定天数，进度用「阶段制」：3 段刻度（调研 / 拆解 / 输出），当前段刻度点亮 `--cinnabar`，其余 `--line`
- 整个区块上下各 1px `--line`，与页面同宽，形成"状态栏"的仪表感

---

## 5. 响应式断点规则

| 断点 | 范围 | 关键变化 |
|:---|:---|:---|
| 桌面 | ≥960px | 内容最大宽 1080px 居中；作品集 3 列；花园条目左右双栏元信息 |
| 平板 | 640–959px | 内容宽 = 视口 - 48px；作品集 2 列；Hero 名 48px；「正在学」两条改上下堆叠 |
| 手机 | <640px | 内容宽 = 视口 - 32px；作品集 1 列；Hero 名 34px；区块标题 26px；3D 降级（§2.3）；导航折叠为汉堡或精简为锚点行 |

通用规则：

- 不用 `vh` 做区块高度（移动端地址栏抖动），首屏用 `100svh`
- 触控目标 ≥44px；hover 效果全部有 `:focus-visible` 等价态
- 图片 `max-width: 100%`；等宽数字用 `font-variant-numeric: tabular-nums` 防跳动
- GSAP ScrollTrigger 在 `<640px` 关闭 #6 行揭示（改简单 fade），减少长列表掉帧

---

## 6. 待 Charlotte 拍板

1. **风格基调**：推荐「墨·朱」暗色主题（朱砂强调色呼应书法项目）。若更想要杂志风浅色，整案可翻转为米白底 + 墨色字 + 朱砂点缀，3D 首屏改用深墨区块嵌套。
2. **3D 第一屏元素**：推荐「知识星群」（线框多面体 + 概念词条，词条可点击跳区块）。备选：极简版仅多面体 + 粒子（§2.4），实现量减半。
3. **标题字体**：推荐 Noto Serif SC 宋体（书法气质）。若想更"科技"，可换 Noto Sans SC 全站黑体，朱砂的东方感仍在。
4. **「正在学」第二条进度表达**：推荐阶段刻度制（调研/拆解/输出）。若希望与财经条对称，也可改成百分比条（需自定一个目标值）。

其余项（色值细节、动效参数、断点数值）按本规范执行即可，DeepSeek 可直接开工。
